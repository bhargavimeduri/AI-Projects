# TRACE — Tiger AI Ultimate v3.0
# EPAIB Batch 05 | Group 4 | IIM Lucknow
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# Python version: 3.9 or above
# Note: If you have a CUDA-compatible NVIDIA GPU, install torch separately:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# ── Core scientific computing ──────────────────────────────────────────────────
numpy>=1.24.0               # Array operations (images are NumPy arrays)
pandas>=2.0.0               # Population report CSV generation
matplotlib>=3.7.0           # Static plot fallback (also used by plotly internally)
seaborn>=0.12.0             # Statistical visualisation (optional)

# ── Image processing ───────────────────────────────────────────────────────────
opencv-python>=4.8.0        # Image loading, drawing, CLAHE, colour operations
Pillow>=10.0.0              # JPEG/PNG/WEBP loading; required by torchvision transforms
scikit-image>=0.21.0        # Additional image utilities

# ── Deep learning — PyTorch ────────────────────────────────────────────────────
# IMPORTANT: The pipeline uses PyTorch (torch), NOT TensorFlow.
# ResNet50, Grad-CAM, and all feature extraction run on PyTorch.
torch>=2.0.0                # PyTorch — runs ResNet50 on CPU or GPU
torchvision>=0.15.0         # Pre-trained ResNet50 weights and image transforms

# Object detection — YOLOv8
ultralytics>=8.0.0          # Loads yolov8n.pt (COCO) and yolov8n-oiv7.pt (Open Images)

# ── ML utilities ───────────────────────────────────────────────────────────────
scikit-learn>=1.3.0         # cosine_similarity for tiger identity matching
imbalanced-learn>=0.11.0    # Optional — class imbalance handling if fine-tuning

# ── Dashboard and visualisation ────────────────────────────────────────────────
plotly>=5.18.0              # Interactive HTML dashboard charts (generate_html_dashboard.py)

# ── Document generation ────────────────────────────────────────────────────────
python-docx>=1.1.0          # Word report generation (generate_run_report_word.py)
python-pptx>=0.6.21         # PowerPoint deck generation (build_ppt.py)
openpyxl>=3.1.0             # Excel panel feedback sheet (generate_panel_feedback_excel.py)

# ── Experiment tracking and notebooks ─────────────────────────────────────────
mlflow>=2.7.0               # Optional — experiment tracking for fine-tuning runs
jupyter>=1.0.0              # Jupyter notebooks (Colab-style local environment)
ipykernel>=6.25.0           # Kernel for Jupyter
ipywidgets>=8.0.0           # Interactive widgets in notebooks

# ── Utilities ──────────────────────────────────────────────────────────────────
tqdm>=4.66.0                # Progress bars for long batch runs
PyYAML>=6.0                 # Config file parsing
python-dotenv>=1.0.0        # Load environment variables from .env files
